import { StyleSheet } from 'react-native';

export const appStyles = StyleSheet.create({
    safeAreaView: {
        flex: 1,
        backgroundColor: '#ffffff',
    },
    scrollViewContent: {
        flexGrow: 1,
        backgroundColor: '#ffffff',
    },
    // Add other styles for your App component here
});
